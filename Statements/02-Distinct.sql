SELECT DISTINCT * FROM users;
/* Aquí "DISTINCT" nos muestra todas las filas de la tabla "users" que sean diferentes entre sí. 
   Cada fila completa debe ser distinta para ser mostrada. */

SELECT DISTINCT age FROM users;
/* De la columna "age", nos muestra los diferentes valores de edad que existen en la tabla, es decir, sin repetir edades. */
